import { createStyles, Theme } from "@material-ui/core/styles";
export default ({  }: Theme) =>
  createStyles({
    root: {},
    form: {
      width: "100%",
      display: "flex",
      flexDirection: "column",
      alignItems: "center"
    }
  });
