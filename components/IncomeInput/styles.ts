import { createStyles, Theme } from "@material-ui/core/styles";
export default ({  }: Theme) =>
  createStyles({
    root: {},
    buttons: {
      width: "100%",
      display: "flex",
      justifyContent: "flex-end"
    }
  });
